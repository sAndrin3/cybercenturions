import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.groopl.app",
  appName: "Groopl",
  webDir: "out",
  bundledWebRuntime: false,
};

export default config;
